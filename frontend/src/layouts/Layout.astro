---
import { ViewTransitions } from "astro:transitions";

export interface Props {
  titulo?: string;
  tema?: "dark" | "light";
}

const { titulo, tema = "light" } = Astro.props;
---

<!doctype html>
<html lang="es">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width" />
    <base href={`${import.meta.env.BASE_URL}`} />
    <link rel="icon" href="./favicon.png" />
    <title>{(titulo && `${titulo} - SITCAV`) || "SITCAV"}</title>
    <ViewTransitions />
  </head>
  <body data-bs-theme={tema} class="overflow-y-scroll">
    <slot />
  </body>
</html>

<style>
  body {
    padding-right: unset !important;
  }
</style>
